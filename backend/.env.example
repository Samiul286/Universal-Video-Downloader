# PORT=8000
# BIND=0.0.0.0
# DOWNLOAD_PATH= (optional; if set and folder exists, used. Else app uses system Downloads if it exists, or user sets folder in Settings)
# DB_PATH=./storage/downloader.db
# MAX_CONCURRENT=2
# QUEUE_MAX_SIZE=100
# FFMPEG_PATH=
# PROXY=
# COOKIES_PATH=
# FRONTEND_URL= (optional; production frontend URL for CORS, e.g., https://your-app.vercel.app)
